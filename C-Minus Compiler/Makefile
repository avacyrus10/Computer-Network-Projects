PYTHON := python3
SCRIPT := compiler.py

run: $(SCRIPT)
	$(PYTHON) $(SCRIPT)

clean:
	@echo "No files to clean."

help:
	@echo "Usage:"
	@echo "    make run     - Run the Python compiler (python3 compiler.py)"
	@echo "    make clean   - Clean up (optional)"
